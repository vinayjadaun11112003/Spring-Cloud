server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hoteldb
    username: root
    password: Kk@8871591738
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka