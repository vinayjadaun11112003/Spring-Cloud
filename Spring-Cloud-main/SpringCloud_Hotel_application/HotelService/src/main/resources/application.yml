server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hoteldb
    username: root
    password: Kk@8871591738

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  config:
    import: configserver:http://localhost:8085
